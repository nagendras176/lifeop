// TODO: Implement Drift table definition
// This file will contain the project table schema
// For now, it's a placeholder

class ProjectsTable {
  // TODO: Define table columns and constraints
  // Example structure:
  // - id (String, primary key)
  // - name (String, not null)
  // - description (String, nullable)
  // - color (int, not null)
  // - createdAt (DateTime, not null)
  // - updatedAt (DateTime, not null)
  // - isActive (bool, not null)
}
